# 更新日志

## v1.3.0

### 输入框改版：模型与思考强度就地切换

输入框改成 DSH / ChatGPT 的形态：**编辑器占上半部分，控制行集中在下方**，
两个最常用的选择不必再绕到设置页。

- **输入框布局** - 输入区最多 6 行（原 3 行）后内部滚动；输入框改为 24dp 圆角、
  1dp 描边与 10/8dp 内边距，控制行依次是「附件 +」「当前模型」「思考强度」「发送」
- **模型选择** - 点击输入框里的模型胶囊直接弹出模型列表，选中即生效并持久化，
  副标题写明「对接下来发送的消息生效」；还没有模型时提示去
  「设置 → 模型管理」添加
- **模型名显示** - 胶囊只显示模型标识的最后一段（`deepseek/deepseek-chat` 显示
  `deepseek-chat`），仍然过长的按真实文本测量二分截断并补省略号；
  抽屉与 `/model` 弹窗仍显示完整名称
- **思考强度选择** - 新增带大脑图标的档位胶囊（关闭 / 自动 / 低 / 中 / 高 / 最高），
  选择后立即写入 AI 行为设置，与设置页里的同一项保持一致
- **抽屉高度** - 两个抽屉最高 400dp、超出部分在面板内滚动，不再铺满整屏，
  内容少时按内容收缩

### 沉浸式系统栏与安全区

- **系统栏透明** - 状态栏与导航栏改为透明并关闭系统强制对比度，
  背景由应用自己绘制到屏幕边缘
- **安全区适配** - 底部弹层与抽屉按真实导航栏高度留白，
  文件抽屉的最后一行、底部面板的最后一项不再被三键导航栏遮住

### 流式输出与结束标记

- **文本不再重复** - 流式文本此前同时写进答案气泡和时间线，边流边显示两遍、
  又在完成时消失；现在只在轮次推进或出现工具调用时才落成「过程块」，
  答案全程只有一份
- **结束标记过滤** - 模型泄漏的结束标记（`<end_of_turn>`、`<｜end▁of▁sentence｜>`、
  `<|im_end|>`、`<end_of_text>` 等）不再进入回答，标记之后的内容一并丢弃；
  被 SSE 分片切开的标记同样能识别，非流式响应也做同样处理
- **结束判定** - `finish_reason` 视为本轮结束信号，代理不发送 `data: [DONE]` 时
  不会一直停在「思考中」；同一批事件会全部消费完再结束，
  末尾的 usage 分片不再丢失，token 统计更准

### 记忆

- **记忆标题** - `memory_update` 工具新增必填 `title`（≤40 字，超长自动截断加省略号），
  记忆列表与详情以标题为主、正文为辅；自动提取的记忆没有标题，仍回退为正文预览
- **旧库迁移** - 打开旧数据库时自动为 `memories` 表补上 `title` 列，已有数据不受影响
- **工具卡片** - 记忆工具有了自己的卡片：书本图标 + 「已保存记忆 〈标题〉」，
  取代此前复用的「已读取」，展开可看完整语句

### 性能

- **工具卡片记忆化** - 时间线每帧都会重新推导工具卡片，子代理与流水线卡片还要拷贝
  整份运行输出并解码进度；现在按事件与运行版本的指纹缓存，
  运行中的卡片照常刷新，已结束的直接命中缓存
- **Markdown 解析缓存** - 长篇回复的 Markdown 不再每帧重新解析（48 条缓存）

### 其他

- **签名与版本** - 沿用原签名，可覆盖安装升级；`versionCode` 34、`versionName` 1.3.0
- **回归覆盖** - 原生测试新增结束标记过滤（含分片）、记忆标题校验与截断、
  记忆工具卡片映射的断言

## v1.2.9-max

### 从 Java 全量迁移到 C++23 与 HuxerUI

这一版不是功能增量，而是**把整个应用从 Java/Android View 重写为 C++23 + HuxerUI**。
界面、交互和可观察行为按 1:1 复刻，除下面明确列出的例外外，功能与旧版一致。

- **技术栈替换** - 原来的 Android 原生 View 与 Java 业务代码全部换成 C++23 严格模式
  （`-std=c++2b`，关闭 GNU 扩展）与 HuxerUI 0.3.0；工程分为
  `domain → application（含 ports）→ infrastructure → presentation` 四层，
  依赖方向单向，业务规则不依赖平台与界面框架
- **功能等价** - 聊天、会话与文件抽屉、模型管理、AI 行为、输入、主题、输出与浏览、
  安全、提示词模板、工具调用、存储统计、错误日志、后台保活、记忆、
  扩展（Agent/MCP/Skills/LineCode 包）、Skill Hub、终端提供者、数据互导、
  教程、开源许可、内置浏览器、Markdown 渲染，均已迁移并可用
- **上下文压缩** - 自动压缩的硬触发（80%）与软触发（50%）改为使用**服务端上报的
  token 用量**驱动；工具循环中途的压缩会写入会话并显示「压缩」进度块，
  摘要持久化，重启后不再重复压缩
- **子代理** - 子代理的工具调用现在与主代理走**同一套审批**，会在界面上弹出确认；
  自定义 Agent 扩展可作为 `agentx_<slug>` 工具被模型直接调用
- **数据互导** - `.linecode` 归档两个方向都已实测：旧版导出可导入本版，
  本版导出也能被旧版正确还原（模型、会话、设置逐项一致）
- **保活** - Android 保留前台服务、Wake Lock 与电池优化白名单入口；
  Windows 上这些入口在编译期即被移除，不会显示
- **签名与版本** - 沿用原签名，可覆盖安装升级；`versionCode` 33、`versionName` 1.2.9-max

### 明确的例外

- **无障碍（Accessibility / 手机控制）整体不迁移** - 相关界面入口、服务、权限与
  「手机控制」模式已全部删除，这是**有意为之**，不是缺失
- **输入框占位提示** - 旧版输入框内的浅灰提示文字（如 `如 GPT-4o`、
  `https://api.example.com/v1`）在本版渲染层不显示；HuxerUI 目前未提供
  占位文字能力，属渲染层限制
- **长页面行距** - 旧版正文默认 2dp 行距，本版渲染层不提供行距设置，
  因此长页面下方会有轻微的位置漂移，非布局错误
- **Windows 构建** - 已做编译期静态验证（平台能力在编译期裁剪），
  但未在真实 Windows 环境构建运行

### 除上述例外，其余无已知问题

功能回归覆盖 23 个场景的同机截图比对，**功能失败为 0**；原生测试全部通过。
